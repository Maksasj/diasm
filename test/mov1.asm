.model tiny

.code
org 100h

start:
    ;; mov scenario 1
    mov al, al
    mov cl, cl
    mov dl, dl
    mov bl, bl
    mov ah, ah
    mov ch, ch
    mov dh, dh
    mov bh, bh
    mov ax, ax
    mov cx, cx
    mov dx, dx
    mov bx, bx
    mov sp, sp
    mov bp, bp
    mov si, si
    mov di, di

end start